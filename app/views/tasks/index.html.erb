<% if logged_in? %>
	<p>ログイン完了ユーザ: <%= current_user.name %></p>
<h1>TODOリスト</h1>

<ul>
	<% @tasks.each do |task| %>
		<li><%= link_to task.content, task_path(task) %> &lt; <%= task.status %></li>
	<% end %>
</ul>

<% else %>
	<div class="center jumbotron">
		<div class="text-center">
			<h1>さあ、やることを整理しよう</h1>
			<%= link_to '新規ユーザ登録', signup_path, class: 'btn btn-lg btn-primary' %>
		</div>
	</div>
<% end %>