<div class="row">
	<div class="panel-heading">
		<h3 class="panel-title"><%= @user.name %></h3>
	</div>

	<div class="col-xs-8">
	  <ul class="nav nav-tabs nav-justified">
	  	<li class="<%= 'active' if current_page?(user_path(@user)) %>"><%= link_to user_path(@user) do %>TODOリスト<span class="badge"><%= @count_taskposts %></span><% end %></li>
 	 </ul>
 	 <%= render 'tasks/tasks', tasks: @tasks %>
	</div>
</div>